# Environment Variable Psychic

Debug missing or incorrect environment variables that cause silent failures. The psychic scans your .env file and compares it with actual environment variables. Run: `node env-psychic.js`